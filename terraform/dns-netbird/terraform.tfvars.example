# Copy this file to terraform.tfvars and fill in real values.
# terraform.tfvars is gitignored — never commit it.

hcloud_token    = "YOUR_HETZNER_API_TOKEN_HERE"
ns1_server_type = "cx22"
ns2_server_type = "cx22"

admin_ssh_ips = [
  "YOUR_ADMIN_IP/32",
]
