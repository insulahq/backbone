---
# Provision k3s cluster on admin1 and deploy management API as PoC
# Usage: ansible-playbook -i inventory/hosts.yml provision_k3s.yml

- name: Provision k3s cluster on admin1
  hosts: k3s_control
  become: true
  roles:
    - k3s
    - management_api_k3s
