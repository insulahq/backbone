---
- name: restart postgresql
  community.docker.docker_compose_v2:
    project_src: "{{ postgresql_install_dir }}"
    state: present
    recreate: always

- name: restart pgproxy-host
  systemd:
    name: pgproxy-host.service
    state: restarted
