---
galaxy_info:
  role_name: postgresql_repmgr
  description: PostgreSQL 18 with repmgr 5.5 HA (streaming replication + auto-failover)
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - trixie
dependencies: []
  # Removed: common and traefik meta-dependencies caused out-of-order execution
  # in Phase 1 (traefik not yet deployed). site.yml enforces correct ordering.
