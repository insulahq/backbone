---
# NetBird Management Role — Handlers

- name: restart netbird
  community.docker.docker_compose_v2:
    project_src: "{{ netbird_install_dir }}"
    state: present
    recreate: always

- name: reload systemd
  systemd:
    daemon_reload: true
