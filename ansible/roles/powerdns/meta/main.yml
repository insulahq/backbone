---
galaxy_info:
  role_name: powerdns
  description: PowerDNS 4.9 Native mode with shared PostgreSQL HA backend
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - trixie
dependencies: []
  # Removed: postgresql_repmgr meta-dependency caused redundant execution and
  # broke primary-first ordering. site.yml enforces correct ordering.
