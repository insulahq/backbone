---
galaxy_info:
  role_name: portainer
  description: Portainer CE — Docker management UI, NetBird-only access
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - trixie
dependencies:
  - role: common
