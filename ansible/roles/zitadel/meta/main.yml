---
galaxy_info:
  role_name: zitadel
  description: Zitadel IAM — central identity provider with PostgreSQL HA backend
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - trixie
dependencies:
  - role: traefik
  - role: postgresql_repmgr
