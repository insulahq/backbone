# Gatus Monitoring Configuration — {{ inventory_hostname }}
# Generated by Ansible — do not edit manually.
# To add notification targets, set variables in group_vars/all.yml
# and re-deploy: ansible-playbook -i inventory/hosts.yml deploy-gatus.yml

{% set peer = groups['dns_servers'] | difference([inventory_hostname]) | first %}
{% set is_primary = (inventory_hostname == (postgresql_primary_node | default('ns1'))) %}
storage:
  type: postgres
  # Gatus uses lib/pq which doesn't support target_session_attrs or multi-host DSN.
  # On the PG primary: connect to local container via Docker DNS.
  # On the PG standby: connect to the primary via WireGuard IP (local PG is read-only).
{% if is_primary %}
  path: "postgres://{{ gatus_db_user }}:{{ gatus_db_password }}@postgresql:5432/{{ gatus_db_name }}?sslmode=disable"
{% else %}
  path: "postgres://{{ gatus_db_user }}:{{ gatus_db_password }}@{{ hostvars[postgresql_primary_node | default('ns1')]['wireguard_ip'] }}:5432/{{ gatus_db_name }}?sslmode=disable"
{% endif %}

ui:
  title: "Hosting Platform"
  header: "Infrastructure Status"

# ============================================================================
# Notification targets
# ============================================================================
# Configure in group_vars/all.yml via gatus_alerting_* variables.
# Each provider uses default-alert which auto-applies to ALL endpoints.
# Re-deploy after changing: ansible-playbook -i inventory/hosts.yml deploy-gatus.yml
alerting:
{% if gatus_alerting_slack_webhook %}
  slack:
    webhook-url: "{{ gatus_alerting_slack_webhook }}"
    default-alert:
      enabled: true
      failure-threshold: 1
      success-threshold: 2
      send-on-resolved: true
{% endif %}
{% if gatus_alerting_discord_webhook %}
  discord:
    webhook-url: "{{ gatus_alerting_discord_webhook }}"
    default-alert:
      enabled: true
      failure-threshold: 1
      success-threshold: 2
      send-on-resolved: true
{% endif %}
{% if gatus_alerting_ntfy_url and gatus_alerting_ntfy_topic %}
  ntfy:
    url: "{{ gatus_alerting_ntfy_url }}"
    topic: "{{ gatus_alerting_ntfy_topic }}"
    default-alert:
      enabled: true
      failure-threshold: 1
      success-threshold: 2
      send-on-resolved: true
{% endif %}
{% if gatus_alerting_email_host %}
  email:
    host: "{{ gatus_alerting_email_host }}"
    port: {{ gatus_alerting_email_port }}
    username: "{{ gatus_alerting_email_username }}"
    password: "{{ gatus_alerting_email_password }}"
    from: "{{ gatus_alerting_email_from }}"
    to: "{{ gatus_alerting_email_to }}"
    default-alert:
      enabled: true
      failure-threshold: 1
      success-threshold: 2
      send-on-resolved: true
{% endif %}
{% if gatus_alerting_telegram_token and gatus_alerting_telegram_id %}
  telegram:
    token: "{{ gatus_alerting_telegram_token }}"
    id: "{{ gatus_alerting_telegram_id }}"
    default-alert:
      enabled: true
      failure-threshold: 1
      success-threshold: 2
      send-on-resolved: true
{% endif %}
{% if gatus_alerting_pagerduty_integration_key %}
  pagerduty:
    integration-key: "{{ gatus_alerting_pagerduty_integration_key }}"
    default-alert:
      enabled: true
      failure-threshold: 1
      success-threshold: 2
      send-on-resolved: true
{% endif %}
{% if gatus_alerting_custom_url %}
  custom:
    url: "{{ gatus_alerting_custom_url }}"
    method: POST
    body: |
      {
        "title": "[ALERT_TRIGGERED_OR_RESOLVED] - [ENDPOINT_GROUP]/[ENDPOINT_NAME]",
        "description": "[RESULT_ERRORS]"
      }
    default-alert:
      enabled: true
      failure-threshold: 1
      success-threshold: 2
      send-on-resolved: true
{% endif %}

# ============================================================================
# External endpoints — receive pushes from infrastructure scripts
# ============================================================================
# platform-alert.sh and entrypoint-wrapper.sh push status updates here.
# Key format: {group}_{name} (e.g., ns1_backup, ns2_disk)
# Alerts auto-applied from alerting providers with default-alert enabled.
external-endpoints:
{% for host in groups['dns_servers'] %}
  - name: backup
    group: {{ host }}
    token: "{{ gatus_external_token }}"
    conditions:
      - "[CONNECTED] == true"
  - name: disk
    group: {{ host }}
    token: "{{ gatus_external_token }}"
    conditions:
      - "[CONNECTED] == true"
  - name: docker
    group: {{ host }}
    token: "{{ gatus_external_token }}"
    conditions:
      - "[CONNECTED] == true"
  - name: postgresql
    group: {{ host }}
    token: "{{ gatus_external_token }}"
    conditions:
      - "[CONNECTED] == true"
  - name: repmgr
    group: {{ host }}
    token: "{{ gatus_external_token }}"
    conditions:
      - "[CONNECTED] == true"
{% endfor %}

# ============================================================================
# Active monitoring — Gatus probes these endpoints directly
# ============================================================================
endpoints:
{% for host in groups['dns_servers'] %}
  - name: "DNS"
    group: {{ host }}
    url: "{{ hostvars[host]['ansible_host'] }}"
    dns:
      query-name: "{{ platform_domain }}"
      query-type: A
    conditions:
      - "[DNS_RCODE] == NOERROR"
    interval: 60s
{% endfor %}
  - name: "Traefik"
    group: platform
    url: "https://{{ platform_domain }}"
    client:
      insecure: true
    conditions:
      - "[STATUS] < 500"
      - "[CERTIFICATE_EXPIRATION] > 720h"
    interval: 60s
{% if zitadel_domain is defined %}
  - name: "Zitadel"
    group: platform
    url: "https://{{ zitadel_domain }}/debug/ready"
    client:
      insecure: true
    conditions:
      - "[STATUS] == 200"
      - "[CERTIFICATE_EXPIRATION] > 720h"
    interval: 60s
{% endif %}
{% if netbird_dashboard_domain is defined %}
  - name: "NetBird"
    group: platform
    url: "https://{{ netbird_dashboard_domain }}"
    client:
      insecure: true
    conditions:
      - "[STATUS] < 500"
      - "[CERTIFICATE_EXPIRATION] > 720h"
    interval: 60s
{% endif %}
  - name: "Gatus"
    group: platform
    url: "https://{{ gatus_domain }}/health"
    client:
      insecure: true
    conditions:
      - "[STATUS] == 200"
      - "[CERTIFICATE_EXPIRATION] > 720h"
    interval: 60s
